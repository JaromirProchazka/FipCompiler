type number {
    Integer(int value);
}

type list {
    Nil;
    Cons(type Tagged value, type list tail);
}

type stree {
    NodeL(type stree left, type Integer value, type stree right);
    Root;
    NodeR( type stree  left , type Integer value, type stree  up);
}

int main [int argc, char** argv] = 0